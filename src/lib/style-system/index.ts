/**
 * Style System - Public API
 *
 * 5-level cascading style system:
 * PROJECT → DOCUMENT → PAGE → BLOCK → CHARACTER
 */

export * from './types'
export { StyleResolver, styleResolver } from './resolver'
